#ifndef MATERIALH
#define MATERIALH

#include <stdbool.h>
#include "ray.h"
#include "vec3.h"

typedef enum { METAL = 0, DIELECTRIC, DIFFUSE } EMaterialType;

/*
typedef struct {
	MaterialType material_type;
} Material;
*/

#endif
